class Snake {
	constructor(head, body, x, y) {
		this.head = head;
		this.body = body;
		this.x = x;
		this.y = y;
	}

	move(direction) {
		if (snake.body.length !== 0) {

		} else {
			
		}
	}
}